server:
  port: 11300
spring:
  application:
    name: pet-service-consumer
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  profiles:
    active: dev
management:
  endpoints:
    web:
      exposure:
        include: '*'
