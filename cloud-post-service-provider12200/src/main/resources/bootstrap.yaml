server:
  port: 12200
spring:
  application:
    name: post-service-provider
  profiles:
    active: dev
management:
  endpoints:
    web:
      exposure:
        include: '*'
#  health:
#    db:
#      enabled: false