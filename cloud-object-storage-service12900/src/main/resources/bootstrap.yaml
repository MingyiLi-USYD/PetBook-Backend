server:
  port: 12900
spring:
  application:
    name: object-storage-service
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  profiles:
    active: dev


management:
  endpoints:
    web:
      exposure:
        include: '*'
