server:
  port: 12600
spring:
  application:
    name: interaction-service-provider
  profiles:
    active: dev
management:
  endpoints:
    web:
      exposure:
        include: '*'
#  health:
#    db:
#      enabled: false